import { FuseRouteItemType } from '@fuse/utils/FuseUtils';

/**
 * ihrs Auth role value
 * its value is null, which means it will be accessible by everyone
 */
const ihrsAuth: FuseRouteItemType['auth'] = null;

export default ihrsAuth;
